# MCP Router 服务器部署分析报告

## 项目概述

**MCP Router** 是一个用于管理 Model Context Protocol (MCP) 服务器的统一管理应用程序。

## 一、部署架构分析

### 1.1 核心架构

根据项目代码分析，MCP Router 的架构如下：

- **主要技术栈**: Electron + React + TypeScript
- **应用类型**: 跨平台桌面应用（Windows 和 macOS）
- **内置服务**: 嵌入式 HTTP API 服务器

### 1.2 项目结构

```
mcp-router/
├── apps/
│   ├── electron/         # Electron 桌面应用
│   └── cli/              # 命令行工具
├── packages/
│   ├── ui/               # UI 组件库
│   ├── shared/           # 共享代码
│   └── remote-api-types/ # 远程 API 类型定义
```

## 二、部署模式分析

### 2.1 当前支持的部署方式

#### ✅ **桌面客户端部署（主要方式）**

**部署方式**：
- 下载安装包（.dmg / .exe）
- 本地安装和运行

**特点**：
- ✅ 完整功能支持
- ✅ 数据本地存储（隐私保护）
- ✅ 原生系统集成
- ✅ 独立运行，无需网络

**下载地址**：
- GitHub Releases: https://github.com/mcp-router/mcp-router/releases

---

#### ⚠️ **HTTP API 服务（本地服务模式）**

**技术实现**：
```typescript
// 文件位置: apps/electron/src/main.ts
// HTTP 服务器在端口 3282 上启动
mcpHttpServer = new MCPHttpServer(serverManager, 3282, aggregatorServer);
await mcpHttpServer.start();
```

**服务端点**：
- 默认端口: `3282`
- API 端点: `http://localhost:3282/mcp`
- SSE 端点: `http://localhost:3282/mcp/sse`

**访问方式**：
```bash
# 设置访问令牌
export MCPR_TOKEN="mcpr_your_token"

# 使用 CLI 连接
npx @mcp_router/cli connect

# 或指定自定义 URL
npx @mcp_router/cli connect --url http://localhost:3282/mcp
```

**特点**：
- ✅ 提供 RESTful API 接口
- ✅ 支持 SSE (Server-Sent Events) 实时通信
- ⚠️ **仅限本地访问** (localhost)
- ⚠️ **无 Web UI 界面**
- ⚠️ 需要 Bearer Token 认证
- ⚠️ 依赖 Electron 桌面应用运行

**适用场景**：
- CLI 工具连接
- 本地开发集成
- 与其他本地工具对接（Claude、Cline、Windsurf、Cursor 等）

---

### 2.2 不支持的部署方式

#### ❌ **独立 Web 服务器部署**

**分析结论**：
- ❌ **无独立的 Web 应用**
- ❌ **无浏览器 UI 界面**
- ❌ **无 Docker 容器化支持**
- ❌ **无云端部署方案**

**原因**：
1. **UI 依赖 Electron**：用户界面完全基于 Electron 框架构建，使用 Electron 特有的 API
2. **本地数据存储**：应用使用 SQLite 本地数据库，数据存储在用户设备上
3. **系统集成**：深度集成操作系统功能（系统托盘、菜单栏、文件系统访问等）
4. **架构设计**：HTTP 服务器仅作为 API 桥接层，不提供 Web UI

## 三、远程工作区功能

### 3.1 Remote Workspace 概述

MCP Router 支持 **Remote Workspace**（远程工作区）功能，这是一个连接到远程后端服务的功能，但**仍然需要桌面应用**作为客户端。

#### 架构说明

```typescript
// 工作区类型定义：packages/shared/src/types/workspace.ts
export interface Workspace {
  id: string;
  name: string;
  type: "local" | "remote";  // 支持本地和远程两种类型
  remoteConfig?: {
    apiUrl: string;           // 远程 API 地址
    authToken?: string;       // 认证令牌
  };
  displayInfo?: {
    avatarUrl?: string;
    teamName?: string;
  };
}
```

#### 工作原理

1. **本地工作区（Local Workspace）**：
   - 数据存储在本地 SQLite 数据库
   - MCP 服务器在本地运行
   - 完全离线工作

2. **远程工作区（Remote Workspace）**：
   - 数据存储在远程服务器
   - 通过 HTTP API 与远程后端通信
   - 需要网络连接和认证
   - 桌面应用作为客户端界面

### 3.2 如何部署 Remote Workspace

#### 方案一：使用官方云服务（推荐）

MCP Router 提供官方云服务托管：

```
官方 API 地址: https://mcp-router.net/api
```

**使用步骤**：

1. **访问官方网站**：https://mcp-router.net/profile
2. **注册/登录账户**：创建您的团队账户
3. **在桌面应用中创建远程工作区**：
   - 打开 MCP Router 桌面应用
   - 创建新工作区（Workspace）
   - 选择类型：`Remote`
   - 输入 API URL：`https://mcp-router.net/api`
   - 登录您的账户获取认证令牌
4. **切换到远程工作区**：选择刚创建的远程工作区

**优点**：
- ✅ 无需自己部署服务器
- ✅ 官方维护，稳定可靠
- ✅ 自动备份和同步
- ✅ 团队协作支持

**注意**：
- 可能需要付费订阅（具体以官方定价为准）
- 数据存储在云端，需要信任服务提供商

#### 方案二：自建 Remote Workspace 后端（高级）

如果您需要完全控制数据，可以自建兼容的后端服务。

**⚠️ 重要说明**：
- 当前项目代码中**没有独立的后端服务实现**
- 需要根据 MCP Router 的 API 规范自行开发后端
- 这是一个高级选项，需要较强的开发能力

**技术要求**：

1. **实现兼容的 HTTP API**：
   ```typescript
   // 需要实现的主要 API 端点：
   - GET/POST  /api/servers      // MCP 服务器管理
   - GET/POST  /api/settings     // 设置管理
   - GET/POST  /api/logs         // 日志管理
   - GET/POST  /api/auth         // 认证接口
   - GET/POST  /api/workspaces   // 工作区管理
   ```

2. **数据库**：
   - 兼容 MCP Router 的数据模型
   - 支持多用户/多租户

3. **认证系统**：
   - 实现 ****** 认证
   - 管理用户权限

4. **API 规范**：
   - 参考项目代码中的类型定义：`packages/shared/src/types/`
   - 查看平台 API 文档：`docs/adr/PLATFORM_API.md`

**部署步骤**（示例）：

```bash
# 1. 开发后端服务（需要自己实现）
# 创建一个兼容 MCP Router API 的服务
# 可以使用 Node.js + Express、Python + FastAPI 等

# 2. 部署到服务器
# 使用 Docker、云服务等方式部署

# 3. 配置 HTTPS 和域名
# 确保使用 HTTPS 加密通信

# 4. 在桌面应用中配置
# API URL: https://your-domain.com/api
# Auth Token: 从您的后端系统获取
```

**开发参考**：

```typescript
// 参考项目中的远程 API 实现
// apps/electron/src/renderer/platform-api/remote-platform-api.ts

export class RemotePlatformAPI implements PlatformAPI {
  private apiBaseUrl: string;
  private authToken: string;

  // 实现各个 API 域
  app: AppAPI;
  server: ServerAPI;
  settings: SettingsAPI;
  // ... 等等
}
```

### 3.3 Remote Workspace 的限制

根据代码分析，远程工作区有以下限制：

1. **工具管理限制**：
   ```typescript
   // 部分功能在远程工作区中暂不支持
   "Tool management is not yet supported for remote workspaces."
   ```

2. **必须使用桌面应用**：
   - ⚠️ 无法直接通过浏览器访问
   - ⚠️ UI 界面仍在 Electron 客户端

3. **网络依赖**：
   - 需要稳定的网络连接
   - API 响应延迟影响体验

### 3.4 本地 vs 远程工作区对比

| 特性 | Local Workspace | Remote Workspace |
|------|----------------|------------------|
| 数据存储 | 本地 SQLite | 远程服务器 |
| 网络需求 | 无需网络 | 需要网络 |
| 多设备同步 | ❌ 不支持 | ✅ 支持 |
| 团队协作 | ❌ 不支持 | ✅ 支持 |
| 隐私性 | ✅ 完全本地 | ⚠️ 数据在云端 |
| 客户端 | 桌面应用 | 桌面应用 |
| 功能完整性 | ✅ 全功能 | ⚠️ 部分限制 |

**结论**：
- ✅ Remote Workspace 支持数据云端存储和多设备同步
- ⚠️ 仍需要桌面应用作为客户端，不能直接浏览器访问
- ⚠️ 如需自建后端，需要自行开发兼容的 API 服务

## 四、浏览器访问测试

### 4.1 HTTP API 端点测试

如果您已安装并运行 MCP Router 桌面应用，可以测试本地 HTTP API：

**测试步骤**：

1. **启动 MCP Router 桌面应用**

2. **在应用中生成访问令牌**：
   - 打开 MCP Router
   - 添加自定义应用（Custom App）
   - 获取生成的 token（格式：`mcpr_xxxxx`）

3. **使用 curl 测试 API**：
```bash
# 替换 YOUR_TOKEN 为实际令牌
curl -H "Authorization: Bearer mcpr_YOUR_TOKEN" \
     http://localhost:3282/mcp
```

4. **浏览器访问限制**：
- ⚠️ 浏览器无法直接访问 `http://localhost:3282`
- ⚠️ 该端点不提供 HTML 页面
- ⚠️ 仅返回 JSON 格式的 MCP 协议数据
- ⚠️ 需要正确的认证头才能访问

### 4.2 无法通过浏览器访问的原因

1. **无 Web 前端**: 3282 端口只提供 API，没有 HTML/CSS/JavaScript 的 Web 界面
2. **认证要求**: 所有请求必须带 Bearer Token
3. **协议限制**: 端点返回的是 MCP 协议数据，不是 Web 页面
4. **CORS 配置**: 虽然启用了 CORS，但主要用于本地工具集成

## 五、CLI 工具说明

### 5.1 Connect 命令（Stdio → HTTP）

```bash
# 连接到本地 MCP Router
export MCPR_TOKEN=your_access_token
npx @mcp_router/cli connect

# 连接到自定义服务器
npx @mcp_router/cli connect --url https://example.com:8080/mcp
```

**用途**：将 stdio 协议转换为 HTTP，供其他工具使用

### 5.2 Serve 命令（HTTP → Stdio）

```bash
# 在 3283 端口启动 HTTP 服务器
npx @mcp_router/cli serve npx @modelcontextprotocol/server-filesystem /path/to/dir

# 使用自定义端口
npx @mcp_router/cli serve --port 8080 python my-mcp-server.py

# 启用认证
npx @mcp_router/cli serve --token secret123 python my-mcp-server.py
```

**用途**：将 stdio 协议的 MCP 服务器暴露为 HTTP 接口

**注意**：
- ⚠️ 这仍然不是 Web UI 部署
- ⚠️ 只是协议转换工具
- ⚠️ 无浏览器界面

## 六、总结与建议

### 6.1 部署模式总结

| 部署方式 | 支持情况 | 访问方式 | 适用场景 |
|---------|---------|---------|---------|
| 桌面客户端（本地工作区） | ✅ 完全支持 | 安装应用 | 个人使用、本地管理、完全离线 |
| 桌面客户端（远程工作区） | ✅ 完全支持 | 安装应用 + 云端后台 | 多设备同步、团队协作 |
| HTTP API | ⚠️ 部分支持 | `localhost:3282` | 本地工具集成 |
| Web 浏览器 UI | ❌ 不支持 | 无 | - |
| Docker 容器 | ❌ 不支持 | 无 | - |
| 独立后端服务 | ⚠️ 需自行开发 | 自定义 API | Remote Workspace 后端 |

### 6.2 核心结论

**MCP Router 是一个桌面应用程序，但支持两种工作模式：**

#### 模式一：本地工作区（Local Workspace）
- ✅ 完全本地运行，数据存储在本地
- ✅ 无需服务器，完全离线工作
- ✅ 隐私性最高，数据不离开本地设备

#### 模式二：远程工作区（Remote Workspace）
- ✅ 支持数据云端存储和多设备同步
- ✅ 支持团队协作
- ⚠️ 仍需要桌面应用作为客户端
- ⚠️ 可使用官方云服务或自建后端

**不支持的方式**：
- ❌ **无 Web UI**：没有独立的浏览器界面
- ❌ **不能纯 Web 部署**：无法作为 Web 应用访问
- ❌ **架构限制**：基于 Electron，UI 无法直接在浏览器中运行

### 6.3 如果需要云端数据同步

**推荐方案：使用 Remote Workspace**

#### 选项 A: 官方云服务（最简单）
```
1. 访问 https://mcp-router.net/profile
2. 注册/登录账户
3. 在桌面应用中创建远程工作区
4. 输入官方 API URL 和认证信息
```
- ✅ 无需自己维护服务器
- ✅ 自动备份和同步
- ✅ 团队协作支持

#### 选项 B: 自建后端服务（高级）
```
1. 开发兼容 MCP Router API 的后端服务
2. 部署到您的服务器（需要 HTTPS）
3. 在桌面应用中配置您的 API URL
```
- ✅ 完全控制数据
- ⚠️ 需要开发和维护成本
- ⚠️ 需要实现完整的 API 兼容层

### 6.4 如果需要纯 Web 访问

如果您确实需要通过浏览器访问 UI（不使用桌面应用），目前有以下选项：

#### 方案 1: 使用 CLI Serve 命令
```bash
# 将 MCP 服务器暴露为 HTTP 服务
npx @mcp_router/cli serve --port 8080 --token YOUR_SECRET python your-mcp-server.py
```
- ✅ 可以在服务器上运行
- ⚠️ 仅提供 API，无 UI
- ⚠️ 需要手动管理服务进程

#### 方案 2: 开发独立 Web 版本（需要重新开发）
- 将 UI 从 Electron 迁移到 React Web
- 实现独立的后端服务
- 添加用户认证和多租户支持
- **工作量巨大，需要重构整个架构**

#### 方案 3: 使用远程桌面
- 在服务器上运行 MCP Router 桌面应用
- 通过 VNC/RDP 远程访问
- ⚠️ 性能开销大，不推荐

### 6.5 官方推荐使用方式

**根据项目设计，推荐的使用方式是**：

#### 个人用户（本地使用）
1. **下载安装桌面应用**：https://github.com/mcp-router/mcp-router/releases
2. **使用本地工作区**：数据完全本地存储，隐私性最高
3. **离线工作**：无需网络连接

#### 团队用户（协作使用）
1. **下载安装桌面应用**：每个成员都安装桌面客户端
2. **创建远程工作区**：使用官方云服务或自建后端
3. **团队协作**：多人共享数据和配置
4. **多设备同步**：在不同设备上无缝切换

#### 开发者集成
1. **使用 CLI 工具**：通过命令行集成其他工具
2. **HTTP API**：本地 API 端点用于工具对接
3. **自定义客户端**：通过 MCP 协议连接

## 七、参考资源

### 7.1 官方资源

- **官网**: https://mcp-router.net
- **GitHub 仓库**: https://github.com/mcp-router/mcp-router
- **下载地址**: https://github.com/mcp-router/mcp-router/releases
- **Discord 社区**: https://discord.com/invite/dwG9jPrhxB

### 7.2 文档

- 英文文档: [README.md](./README.md)
- 中文文档: [README_zh.md](./README_zh.md)
- 日文文档: [README_ja.md](./README_ja.md)
- CLI 工具文档: [apps/cli/README.md](./apps/cli/README.md)

### 7.3 技术架构文档

- [平台 API 架构](./docs/adr/PLATFORM_API.md)
- [Electron 目录结构](./docs/adr/ELECTRON_DIRECTORY_STRUCTURE.md)
- [数据库架构](./docs/adr/database/DATABASE_ARCHITECTURE.md)

## 八、许可证

本项目采用 **Sustainable Use License** 授权，详见 [LICENSE.md](./LICENSE.md)

**关键限制**：
- ✅ 允许个人和内部商业使用
- ✅ 允许修改和分发（非商业目的）
- ❌ 不允许作为收费服务提供
- ❌ 不允许删除或修改许可声明

---

**报告生成时间**: 2026-01-14  
**分析版本**: v0.6.1  
**报告作者**: GitHub Copilot  
